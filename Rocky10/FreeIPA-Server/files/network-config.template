version: 2
ethernets:
  eth0:
    match:
      macaddress: ${INTERFACE_MAC}
    set-name: eth0
    dhcp4: false
    addresses:
      - ${STATIC_IP_CIDR}
    routes:
      - to: 0.0.0.0/0
        via: ${GATEWAY_IP}
    nameservers:
      addresses:
${NAMESERVERS_YAML}
